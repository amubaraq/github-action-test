name: Greet User
# on: push
on: workflow_dispatch #it won't run workflow on push
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
          - name: greet User
            run: echo "Hello world"
          - name: Bye User
            run: echo "Bye Bye User"